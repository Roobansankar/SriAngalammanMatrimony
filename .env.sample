# =============================================================================
# Copy this file to .env and fill in your values.
# Works for both LOCAL development and PRODUCTION VPS deployment.
# =============================================================================

# ---------- Environment ----------
NODE_ENV=production
# For local dev, set to: development

# ---------- Backend / API ----------
PORT=5000

# Database connection (Docker service name or external host)
# LOCAL:  DB_HOST=db (docker service name)
# PROD:   DB_HOST=db (or external MySQL host if not using Docker DB)
DB_HOST=db
DB_USER=matrimony
DB_PASS=matrimony
DB_NAME=sriang

# Public base URL for absolute links in emails, webhooks, and image URLs
# LOCAL:  http://localhost:5000 or http://localhost:3000 (if using proxy)
# PROD:   http://YOUR_VPS_IP:5000 or https://yourdomain.com
API_BASE_URL=http://localhost:5000
BASE_URL=http://localhost:5000

# Auth / JWT (CHANGE THIS IN PRODUCTION!)
JWT_SECRET=change-me-to-a-secure-random-string

# SMTP Configuration (for OTP emails)
SMTP_USER=you@example.com
SMTP_PASS=your-smtp-password
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=465

# Razorpay Payment Gateway
RAZORPAY_KEY_ID=your-razorpay-key
RAZORPAY_KEY_SECRET=your-razorpay-secret

# ---------- Frontend (React Build Args) ----------
# API base ORIGIN used by the frontend (NO trailing /api)
# Docker/nginx proxy setup: leave empty to use same-origin and call /api/*
# Local dev without Docker: set to http://localhost:5000
REACT_APP_API_BASE=

# Socket.io URL (leave empty to use same origin via nginx proxy)
REACT_APP_SOCKET_URL=

# ---------- MySQL Container ----------
# These are used by docker-compose for the MySQL container
MYSQL_ROOT_PASSWORD=password
MYSQL_DATABASE=db-name
MYSQL_USER=matrimony
MYSQL_PASSWORD=matrimony

# Data persistence directory (host path)
# LOCAL:  ./.data/mysql (relative to project root)
# PROD:   /var/lib/matrimony/mysql (or any persistent path on VPS)
MYSQL_DATA_DIR=./.data/mysql

# =============================================================================
# PRODUCTION VPS EXAMPLE
# =============================================================================
# NODE_ENV=production
# API_BASE_URL=http://80.65.208.64:5000
# BASE_URL=http://80.65.208.64:5000
# REACT_APP_API_BASE=
# MYSQL_DATA_DIR=/var/lib/matrimony/mysql
# JWT_SECRET=your-production-secret-key
# =============================================================================
